#!/usr/bin/env bash

set -euo pipefail

cd "$(dirname "$0")/.."

python3 scripts/check_ha_version_sync.py

./scripts/docs-bundle lock --print > /tmp/Gemfile.lock
diff -u docs/Gemfile.lock /tmp/Gemfile.lock

ruff format .
ruff check . --fix
